Bug 1: Fixed crash when searching for a Book ID that doesn't exist (added NULL check).

Bug 2: Fixed "Memory Leak" in the Linked List delete function by using the delete keyword.

Bug 3: Fixed input buffer issue where getline was skipped after cin.
